# ============================================
# Makoto Shinkai Lock Screen
# Complete Configuration with Background
# ============================================

# ============================================
# BACKGROUND IMAGE
# ============================================

background {
    monitor =
    
    # Garden of Words wallpaper
    path = /nix/store/fnlaq20kph90hw04bnjb8lfc10pcbkg3-home-manager-files/Pictures/Wallpapers/Themes/Makoto_Shinkai/938846-1920x1080-desktop-full-hd-makoto-shinkai-wallpaper-image.jpg
    
    # Fallback color if image doesn't load
    color = rgba(26, 35, 50, 1.0)
    
    # Blur for dreamy Shinkai aesthetic
    blur_passes = 3
    blur_size = 7
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.05
}

# ============================================
# TIME DISPLAY - Top Left
# ============================================

label {
    monitor =
    text = <span letter_spacing="4096">$TIME</span>
    color = rgba(232, 244, 248, 0.9)
    font_size = 72
    font_family = Noto Sans CJK JP Light
    
    position = 50, -50
    halign = left
    valign = top
    
    # shadow_passes = 2
    # shadow_size = 15
    # shadow_color = rgba(0, 0, 0, 0.9)
    # shadow_boost = 1.2
}

# ============================================
# DATE DISPLAY - Below Time (Small Size)
# ============================================

label {
    monitor =
    # text = cmd[update:60000] date '+%A, %B %d'
    text = <span letter_spacing="2048">December 28, 2025</span>
    color = rgba(168, 213, 232, 0.6)
    font_size = 14                      # Small size like HTML
    font_family = Noto Sans CJK JP Light
    
    # Position below time: 50px from left, 155px from top (moved down)
    position = 60, -180
    halign = left
    valign = top
    
    # shadow_passes = 1
    # shadow_size = 10
    # shadow_color = rgba(0, 0, 0, 0.8)
}

# ============================================
# MOVIE TITLE - English (Above Input)
# ============================================

label {
    monitor =
    text = YOUR NAME
    text = <span letter_spacing="6144">YOUR NAME</span>

    color = rgba(232, 244, 248, 0.6)
    font_size = 16
    font_family = Noto Sans CJK JP Light
    
    # Position: centered, above Japanese title
    position = 0, 200
    halign = center
    valign = center
    
    # shadow_passes = 1
    # shadow_size = 15
    # shadow_color = rgba(0, 0, 0, 0.8)
}

# ============================================
# MOVIE TITLE - Japanese (Above Input)
# ============================================

label {
    monitor =
    text = <span letter_spacing="8192">君の名は。</span>

    color = rgba(232, 244, 248, 0.95)
    font_size = 42
    font_family = Noto Sans CJK JP Light
    
    # Position: centered, above input field
    position = 0, 160
    halign = center
    valign = center
    
    # shadow_passes = 2
    # shadow_size = 20
    # shadow_color = rgba(0, 0, 0, 0.9)
    # shadow_boost = 1.3
}

# ============================================
# QUOTE - English (Between Title and Input)
# ============================================

label {
    monitor =
    text = <i>"Once in a while when I wake up, I find myself crying."</i>
    color = rgba(232, 244, 248, 0.95)
    font_size = 32
    font_family = Noto Sans CJK JP Light
    
    # Position: centered, between Japanese title and input
    position = 0, -30
    halign = center
    valign = center
    
    # shadow_passes = 2
    # shadow_size = 20
    # shadow_color = rgba(0, 0, 0, 0.9)
    # shadow_boost = 1.3
}

# ============================================
# QUOTE - Japanese (Below English Quote)
# ============================================

label {
    monitor =
    text = たまに、目が覚めると泣いている
    color = rgba(168, 213, 232, 0.7)
    font_size = 20
    font_family = Noto Sans CJK JP Light
    
    # Position: centered, below English quote, above input
    position = 0, -80
    halign = center
    valign = center
    
    # shadow_passes = 1
    # shadow_size = 15
    # shadow_color = rgba(0, 0, 0, 0.8)
}

# ============================================
# PASSWORD INPUT - Center
# ============================================

input-field {
    monitor =
    size = 500, 70
    outline_thickness = 2
    
    inner_color = rgba(43, 62, 80, 0.5)
    outer_color = rgba(91, 159, 216, 0.5)
    check_color = rgba(91, 159, 216, 0.9)
    fail_color = rgba(255, 102, 102, 0.8)
    font_color = rgba(232, 244, 248, 0.9)
    
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    dots_rounding = -1
    
    placeholder_text = <i><span foreground="##a8d5e899">Enter to remember...</span></i>
    
    font_family = Noto Sans CJK JP
    font_size = 18
    
    fade_on_empty = false
    fade_timeout = 0
    hide_input = false
    
    position = 0, -200
    halign = center
    valign = center
    
    rounding = 30
    
    # shadow_passes = 1
    # shadow_size = 3
    # shadow_color = rgba(0, 0, 0, 0.4)
    # shadow_boost = 1.0
}